# Datasets

Store raw and processed datasets and dataset manifests. Ensure sensitive data is not committed.

Structure:

- `raw/` – original data (do not commit sensitive files)
- `processed/` – preprocessed artifacts used for training
- `metadata/` – manifests and README for dataset provenance
