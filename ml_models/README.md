# ML Models

Model implementations and training/evaluation utilities. This directory intentionally contains stubs only — actual model code will be added during implementation.

Structure:

- `models/` – model modules
- `utils/` – helpers for datasets, metrics
