# Backend

FastAPI backend for model serving, inference endpoints, and auxiliary services.

Structure:

- `backend/app/` – FastAPI application package
- `backend/services/` – wrappers for models, lexicon lookup, etc.
