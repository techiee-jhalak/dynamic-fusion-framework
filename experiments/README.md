# Experiments

Configuration files, run logs, and experiment results for reproducible research.

Structure:

- `configs/` – YAML experiment definitions
- `results/` – metrics, model checkpoints
- `runs/` – per-run metadata
